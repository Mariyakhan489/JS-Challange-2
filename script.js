// Coding Challenge # 2

let markWeight = 78;
let markHeight = 1.69;
let johnWeight = 92;
let johnHeight = 1.95;

//  BMI
let markBmi = markWeight / (markHeight * markHeight);
let johnBmi = johnWeight / (johnHeight * johnHeight);

if (markBmi > johnBmi) {
    console.log(`Mark's BMI ${markBmi} is Haigher than John's BMI ${johnBmi}`);
}
else{
    console.log(`John's BMI ${bmiMark} is Haigher than Mark's BMI ${bmiJohn}`);

}